import 'package:flutter/material.dart';

class CartDesktop extends StatelessWidget {
  const CartDesktop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
