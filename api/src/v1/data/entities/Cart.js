'use strict';
class Cart{
    constructor({id, userId, createdAt, updatedAt}){
        this.id = id;
        this.userId = userId;
        this.createdAt = createdAt;
        this.updatedAt = updatedAt;
    }
}